import 'package:flutter/material.dart';

const Color LOADING_OVERLAY_COLOR = Colors.black;
const CircularProgressIndicator LOADING_INDICATOR = CircularProgressIndicator(color: Color(0xccffffff));
const Color LOGIN_TXT_COLOR = Color(0xff737373);
